{
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
    }
}
